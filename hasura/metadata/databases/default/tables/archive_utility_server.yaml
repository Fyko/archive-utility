table:
  name: server
  schema: archive_utility
array_relationships:
- name: archives
  using:
    foreign_key_constraint_on:
      column: server_id
      table:
        name: archive
        schema: archive_utility
